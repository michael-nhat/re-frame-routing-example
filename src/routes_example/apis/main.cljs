(ns routes-example.apis.main
  ;; (:require [])
  )

;;endpoint
(def E)

;; fetch
(defn Ef [method then catch]
  (print 1))

;; popup?
;; default error handler
;; if network error set type network error / api error
;; authentication

;; error: type, err

